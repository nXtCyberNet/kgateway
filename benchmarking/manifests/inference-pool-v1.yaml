apiVersion: inference.networking.x-k8s.io/v1
kind: InferencePool
metadata:
  name: llama-pool
  namespace: default
spec:
  selector:
    matchLabels:
      app: llm-d-sim
  extensionRef:
    name: llama-epp
    kind: EndpointPickerExtension
