apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferencePool
metadata:
  name: llama-pool
  namespace: default
spec:
  selector:
    app: llm-d-sim
  targetPortNumber: 8000
  extensionRef:
    name: llama-epp
    kind: EndpointPickerExtension
