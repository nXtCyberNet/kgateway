apiVersion: batch/v1
kind: Job
metadata:
  name: inference-perf
spec:
  template:
    spec:
      containers:
      - name: perf
        image: {{ .Values.image }}
        env:
        - name: SCENARIO
          value: {{ .Values.scenarioUrl }}
      restartPolicy: Never